function clickinfo() {
	if (lastselection != 'info') {
        // Write a code to get info of the selected gene
	}
}

function details(command)
{
    // to get all the details of the gene including it's id, species name, gene name etc.
}

function cne_mouseover(targets)
{
    // highlige the perticular gene if the mouse is been hovered above the location of the gene on the map
}

function cne_mouseout(targets)
{
    // remove the highlighted part if the mouse is been hovered out 
}

function refresh(){

    // remove all the selected genes an the data displayed on screen and refresh to Iniitial graph

}

function show_graph(id, species_name, gene_name){
    // display the graph on window with their (species name + gene name)
}

function checkboxSpec(id){
    // create check box function to toggle between different graphs based on different properties
	if(checkbox.style.display == 'none'){
	}
    else{
	}
}

function rightClick(spec_id)
{
    // to add functionalities when users right click on different sections
}